You are an expert auditor reading a SOC 1 Type II report.
Below are the first pages of the report, which contain the table of contents.

Your task: identify the service organization name and page ranges that are explicitly supported
by sufficiently detailed table-of-contents entries. Titles may vary between reports, but do not
infer a topic's range from broad entries such as "Description of the System", "Control Activities",
or "Additional Information".

Sections to locate:
1. Independent Service Auditor's Report / Opinion
2. Change Management (IT general controls for program/system changes)
3. Access Management (IT general controls for user access and security)
4. Job Scheduling & Monitoring (IT general controls for batch jobs)
5. Subservice Organizations (third-party providers listed in the report)
6. Complementary User Entity Controls (CUECs). This section may also be named
   "Complementary User Entity Control Considerations", "Complementary User Entity
   Responsibilities", "Customer Responsibilities", "User Control Considerations",
   "User Entity Responsibilities", or similar wording.
7. Complementary Subservice Organization Controls (CSOCs). This section may also
   be named "Complementary Subservice Organization Control Considerations",
   "Complementary Sub-service Organization Controls", "Subservice Organization
   Controls", or similar wording.

Return ONLY valid JSON — no explanation, no markdown fences:
{{
  "system_name": "<service organization name>",
  "opinion_pages": [<start>, <end>],
  "change_mgmt_pages": [<start>, <end>],
  "access_mgmt_pages": [<start>, <end>],
  "job_scheduling_pages": [<start>, <end>],
  "subservice_pages": [<start>, <end>],
  "cuec_pages": [<start>, <end>],
  "csoc_pages": [<start>, <end>]
}}

Return a range only when the TOC specifically identifies that topic and provides enough page
information to determine its start and end. The end may be derived from the next detailed TOC
entry. If the TOC is missing, incomplete, too broad, or does not specifically identify a topic,
set that topic's range to [0, 0]. Do not guess a range and do not treat [0, 0] as proof that the
section is absent: full-report keyword and BM25 retrieval will search for it separately.

TABLE OF CONTENTS:
{toc_text}
